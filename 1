DATA=$(zenity --forms --title='Login' --text=' ' --add-entry='Email' --add-password='Password');
email_login="${DATA%%|*}"; email_password="${DATA#*|}"
echo "$email_login"
echo "$email_password"
if [[ "$email_login" == *"@gmx.com" ]]; then

    imap_server="imap.gmx.com"
fi

echo "$imap_server"
